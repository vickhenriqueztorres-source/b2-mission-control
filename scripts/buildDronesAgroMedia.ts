/**
 * Retained as an explicit blocker for the retired still-to-video workflow.
 * A generated image is a start frame, never proof of temporal video.
 */
throw new Error(
  'STATIC_TAKE_MATERIALIZATION_FORBIDDEN: use Firefly Video and validate the resulting MP4 before render.',
);
